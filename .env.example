# Copy to .env (gitignored) to boot the server against your real Yandex account.
# Easiest: run `npm run set-token` and paste your token — it validates and
# writes this file for you. See scripts/set-token.mjs for how to obtain a token.
#
# Without this, the server runs in demo mode (bundled procedural audio).
YANDEX_TOKEN=
